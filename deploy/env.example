# Copy to deploy/.env and fill with strong passwords.
# Never commit .env.

# MariaDB root password (bench uses it to create the site DB)
DB_ROOT_PASSWORD=change-me

# Administrator password for the newton site (first login)
ADMIN_PASSWORD=change-me